# 上游更新整合记录

日期：2026-09-16。分支：`integrate/upstream-a629602`。

## 对比范围

本地修复基线为 `55ec469`（包含 A/B/C）；原上游基线为 `38a41ea`。
本次 `git fetch origin` 后，主分支新增 1 个提交：

- [`a629602`：加长任务忙碌指示 + 新动画落地自动播放](https://github.com/Ddarjbb/interactive-teaching-agent/commit/a629602)

上游共改动 9 个文件，105 行增加、19 行删除。此次主分支没有新增数学布局算法或生成模型修复。

## 整合决定

| 上游内容 | 本地处理 |
|---|---|
| 页脚、动画面板、知识图长任务提示 | 已整合；按任务编号清理，补上停止、异常、切会话及递归事件处理 |
| 动画生成/修改后自动播放 | 已整合；保留“回到开头”暂停语义，未验证的注入场景预览不触发新代码自动播放 |
| 出题回复恢复时 loading 状态 | 已整合；旧恢复任务不能清掉新任务状态；递归流收到考题也能展示 |
| 图分解期间禁用“全部导入” | 已整合，避免导入正在变化的图 |
| Vite 从 PORT 环境变量读端口 | 已整合；无效端口回退 5173 |
| `.claude/launch.json` 自动端口 | 未复制个人启动器配置；项目本身已支持 PORT 和 `--port` |
| `.claude/settings.local.json` | 未引入作者个人机器路径及扩大的本地读取权限 |
| 后端 views 尾部空行 | 无行为变化，未纳入 |

采用文件级三方合并及本地适配，不用上游文件整体覆盖本地修复。冲突涉及 `StagePanel.tsx` 与 `store.tsx`，均保留本地验证协议、取消邮箱和参数绑定。

## 额外适配

1. 上游仅按任务种类清理提示；本地改为按任务编号清理。同类新任务不会被旧任务的清理动作覆盖。
2. 顶层 SSE 与递归恢复流共用忙碌状态转换，并在 `finally` 中收尾；错误和停止不再留下永久转圈提示。
3. 上游等待提示放在脚本会清空的 canvas 容器里。已移到容器外，避免自建场景清空 React 管理的节点。
4. 本地 A/B/C 后端代码未被此次整合修改。

## 验证

- 前端 18 项：原验证 12 项、可靠性 3 项、上游整合 3 项，全部通过。
- 后端原 58 项回归通过。
- TypeScript 与 Vite 构建通过；仍有原来的大包体积提示。
- StrictMode 真实浏览器界面验证：
  - 新动画自动执行到第二段；回到开头停在第一段，按钮恢复“播放”。
  - 等待提示展示、清理；等待结束载入自建场景后无页面异常。
  - 旧任务清理后，新任务提示仍在。
  - 既有验证结果：正常 passed、被替换 cancelled、新请求 passed、主动取消 cancelled、关闭检查 incomplete。
  - 重叠样例仍为 failed，草稿不定稿。

新增测试入口：`tests/upstream.test.ts`、`tools/upstream-ui.html`。没有调用真实外部模型，也没有截图或导出录屏。

## 其他远程分支

远程另有 `fix/decompose-arrow-alignment`（`48da22e`，箭头与气泡对齐修复），它不属于本次主分支新增提交，仍未合入 `origin/master`。本轮未混入该分支，避免把未验证的独立图布局改动与这次主线更新合并。

更新仅提交本地，没有推送 GitHub。以后同步请继续三方比较，保留本地 A/B/C 修复。
